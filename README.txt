Project to demonstrate working of Observer Pattern in Java
Description :

Takes the input values from file and populates a tree where each node is a subject

Two parallel trees are created  where Nodes are observes with filters.

Any update on the subject node notifies the observer node.